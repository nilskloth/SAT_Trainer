/* Parent view: per-domain mastery, timeline to SATs, AI usage, settings. Built in Phase 6. */

export function renderProgress(el) {
  el.innerHTML = `
    <div class="card">
      <h2 style="margin-top:0;">Grown-ups</h2>
      <p class="muted">Progress by topic, the run-up to May 2027, and settings — coming in Phase 6.</p>
    </div>
  `;
}
