/* Focused training by domain. Trainers ported here in Phase 4. */

export function renderPractice(el) {
  el.innerHTML = `
    <div class="card">
      <h2 style="margin-top:0;">Practice</h2>
      <p class="muted">Reading, grammar, spelling and maths practice — coming in Phase 4.</p>
    </div>
  `;
}
